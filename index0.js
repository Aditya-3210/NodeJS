//require, in common js
//for using import of ES6, we need to change the type from commonJS to module

/* npm install express - will install the corresponding dependencies, 
and will also update the version */ 
//npm install

//running server

/* every time when we make any change, we need to restart the server manually 
to implement those changes, */